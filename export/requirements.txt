# Extra packages export/export_onnx.py needs on top of the training environment
# (torch lives in the repo venv; use bin/py):
#   bin/py -m pip install -r export/requirements.txt
onnx>=1.22
onnxruntime>=1.30
onnxscript>=0.7
